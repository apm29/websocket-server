server:
    servlet:
        context-path: /talk
spring:
    datasource:
        url: jdbc:mysql://localhost:3306/imptt?useUnicode=true&characterEncoding=utf8&characterSetResults=utf8&useSSL=false
        username: root
        password: root
        driver-class-name: com.mysql.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true